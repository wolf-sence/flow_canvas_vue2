备忘录：
绘制顺序：
先实例化children，将children推入障碍物地图，再实例化parent，推入parent；
绘制先绘制parent，再绘制children


问题: 叠放问题和父子顺序推入 障碍物地图问题
现在：由于 首次渲染时，是children实例化后，推入map，然后推父节点
        然后叠放问题，
无限次重绘问题


ctx.scale()后 ，鼠标捕捉的offset不变，


每次绘制前，实例化一个障碍物地图，（map）

然后根据地图 计算路径，

使用在 ManhattanRouter。

障碍物 寻找最短路径










设计思路：
    画布：处理拖动事件，分发单击、双击、拖动事件，边界处理，重绘，下探以及下探动画触发，

    组件：每个组件自己处理自己独特的逻辑，如：自己处理自己的双击事件，单击事件，hover事件，拖选框事件则靠画布传递





流程图 修改事项：
修改：
hover框 距离，色彩更改，
anchor 实心，
step 加入 icon 绘图

node 平台、银行、应用类型不同颜色组件
梯形，角度修改，连接处 使用圆弧
中间的'|'颜色改变

node/step 背景空白处换为 纯白色

node右上角的小节点： debugger：在属性栏勾选
                    备注icon：编写临时性备注，顺带色彩改变


背景色：换色
滚动条 背景色 换色


## 内存问题：
    新建节点，然后删除时，底层 dep 所绑定的 watch没有被删除，dep会被重复叠加